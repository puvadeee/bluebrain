Xbox joypad control of lastest BlueBrain (board without the detachable motor driver and using mbed)

The sub-folders under ./rootfs/ should be copied to /

Put the bot BLE MAC addresses in /boot/cannybots/cannybot[1|2].txt

use the ./sctipys/scan script to find the MAC address of the bots.

Other scripts in ./scripts/ can be use to start|stop services


